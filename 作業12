＃包括< stdio.h中>
＃包括< stdlib.h中>
void  show（int）;
int  main（void）
   {
	int x;
	printf（“ ¿¤¤J¤@Ó¼Æ：”）;
	scanf（“％d ”，＆x）;
	show（x）;
   }
void  show（int x）
   {
	int space;
	for（int o = x; o> 0 ; o--）
	   {
		空間= XO;
		if（space> 0）
		  {
			for（int p = 0 ; p <space; p ++）
			{
			printf（“  ”）;
			}
		  }

		for（int l = 0 ; l <o; l ++）
		  {
			printf（“％c ”，“ * ”）;
		  }
		printf（“ \ n ”）;
	   }
   }
